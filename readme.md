To compile maze.cpp and its dependencies, use the following command
```$ make```

To run the generates executable, use the following command
```mpirun -np 4 ./maze.out -g [bfs/kruskal] -s [dfs/dijkstra]```